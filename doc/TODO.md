1. Setup `/app` for a GH-pages deployed vanilla vite web-app
* implement a simple hash-router 
   * `/` is the main CV w/ links to other stuff
   * `/blog` is the blog index
   * `/blog/:slug` is the blog post
   * `/cv` is the professional CV
   * `/game` is the "playable" CV
* migrate the blog entries
* cleanup old app cruft
