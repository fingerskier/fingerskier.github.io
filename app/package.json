{
  "name": "app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "prebuild": "node scripts/gen-blog-manifest.js && cp -r ../content public/content && cp ../CV.md public/CV.md && cp ../cv.json public/cv.json",
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint \"src/**/*.js\"",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "marked": "^15.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.25.0",
    "eslint": "^9.25.0",
    "gh-pages": "^6.3.0",
    "globals": "^16.0.0",
    "vite": "^6.3.5"
  }
}
